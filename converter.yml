server:
  rootPath: /api/
  type: simple
  applicationContextPath: /
  connector:
    type: http
    port: 8080

assets:
  overrides:
    # Override requests to /dashboard/${1} to instead look in 
    # ${working directory}/src/main/resources/assets/${1}
    /: src/main/java/app/resources/assets/
