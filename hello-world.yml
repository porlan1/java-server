template: Hello, %s!
defaultName: Stranger
server:
  rootPath: /api/
assets:
  overrides:
    # Override requests to /dashboard/${1} to instead look in 
    # ${working directory}/src/main/resources/assets/${1}
    /: src/main/java/com/example/helloworld/resources/assets/

